# Optional machine-learning dependencies for emotion model training
# Keep these separate from the runtime dependencies in requirements.txt.
torch>=2.0,<3.0
torchvision>=0.15,<1.0
scikit-learn>=1.3,<2.0
Pillow>=10.0,<13.0
